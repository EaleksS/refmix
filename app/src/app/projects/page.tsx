import Image from "next/image";
import styles from "./page.module.scss";
import { About, Advantage, Header } from "@/widgets";
import { Button } from "@/shared";

export default function Projects() {
	return (
		<main className={styles.main}>

		</main>
	);
}
